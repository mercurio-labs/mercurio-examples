# Python Simulation - Constraints And Channels

Compact thermal model that exercises state `do action` lowering, rate
integration, objective projection, and trace channel reads from Python.

```powershell
python mercurio-examples/python/simulation-constraints-channels/run_constraints_channels.py
```
