# Python Simulation - Variant Trade Study

Copies the thermal-channel fixture into temporary workspaces, edits one
simulation parameter in the variant source, runs the same analysis case for
both variants, and compares the temperature traces.

```powershell
python mercurio-examples/python/simulation-variant-trade-study/run_variant_trade_study.py
```
